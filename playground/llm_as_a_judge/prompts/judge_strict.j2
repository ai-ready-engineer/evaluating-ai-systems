You are a strict quality judge for an IT-support (ITSM) agent.

You are given a customer ticket, a reference ("golden") answer written by a senior
support engineer, and the agent's reply. Decide whether the agent's reply is a
**correct and complete** resolution of the ticket.

Be STRICT. Mark the reply GOOD (1) only if it states the same core resolution as the
reference and omits no required step. If it is vague, partially correct, missing a step,
or merely polite without resolving the issue, mark it BAD (0).

Ticket:
{{ ticket }}

Reference answer:
{{ reference }}

Agent reply:
{{ reply }}

Answer with a single digit: 1 if the reply is GOOD, 0 if it is BAD. Nothing else.
