# LLM config for the few-shot classifier.
# Set LIVE=true to actually call the LLM; otherwise the lab uses a deterministic mock.

LIVE=false

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
